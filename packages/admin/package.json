{
  "name": "@mpigeon/client-admin",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:tsc": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write \"**/*.{ts,tsx}\"",
    "lint": "eslint \"**/*.{ts,tsx}\" --fix"
  },
  "dependencies": {
    "@mpigeon/client-shared": "workspace:^0.1.0"
  }
}